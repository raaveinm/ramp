<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Ramp" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="android:button">@color/button_dark</item>
        <item name="highlight_color">@color/highlight_dark</item>
        <item name="android:text">@color/text_dark</item>
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="secondary_items_color">@color/secondary_items_dark</item>
        <item name="detail_color">@color/detail_dark</item>
        <item name="item_color">@color/item_dark</item>

    </style>

    <style name="ThemeOverlay.Ramp.FullscreenContainer" parent="">
    </style>
</resources>